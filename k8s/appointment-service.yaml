apiVersion: v1
kind: Service
metadata:
  name: appointment-service-network
spec:
  type: NodePort
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005
      nodePort: 30005 # Hit http://localhost:30005 for this service
