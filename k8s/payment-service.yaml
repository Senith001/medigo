apiVersion: v1
kind: Service
metadata:
  name: payment-service-network
spec:
  type: NodePort
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 5007
      targetPort: 5007
      nodePort: 30007 # Hit http://localhost:30007 for payment-service API
