apiVersion: v1
kind: Service
metadata:
  name: auth-service-network
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30001 # Hit http://localhost:30001 in Postman