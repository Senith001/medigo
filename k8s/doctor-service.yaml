apiVersion: v1
kind: Service
metadata:
  name: doctor-service-network
spec:
  type: NodePort
  selector:
    app: doctor-service
  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004
      nodePort: 30004 # Hit http://localhost:30004 in Postman or browser for doctor-service API
